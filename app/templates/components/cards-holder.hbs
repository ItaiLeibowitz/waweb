<!--
{{card-position-fixer resetAction=(action 'resetAllCards')}}
-->
{{#each childWrappers as |wrapper|}}
	{{photo-card model=wrapper.item  index=wrapper.index withInfo=wrapper.withInfo isAd=wrapper.isAd
	isExpanded=wrapper.isExpanded resultCard=wrapper.resultCard
	resetInfoAction=(action 'resetAllCardsInfo')
	resetSizeAction=(action 'resetAllCardsSize')}}
{{/each}}
