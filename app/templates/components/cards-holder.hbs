<!--
{{card-position-fixer resetAction=(action 'resetAllCards')}}
-->
{{#each childWrappers as |wrapper|}}
	{{photo-card model=wrapper.item  index=wrapper.index  isAd=wrapper.isAd
	resultCard=wrapper.resultCard isExpanded=wrapper.isExpanded
	withIndex=withIndex last=@last
	resetSizeAction=(action 'resetAllCardsSize')}}
{{/each}}
